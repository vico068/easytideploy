FROM python:3.12-slim

WORKDIR /app

COPY . .

ARG BUILD_COMMAND="pip install --no-cache-dir -r requirements.txt"
RUN if [ -f requirements.txt ]; then sh -c "$BUILD_COMMAND"; fi

EXPOSE ${PORT:-8000}

ARG START_COMMAND="python main.py"
CMD sh -c "$START_COMMAND"
